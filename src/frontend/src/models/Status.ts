/**
 * Status of the request
 * 
 * @enum {number}
 * @export Status
 * @property {number} LOADING - loading state
 * @property {number} LOADED - loaded state
 * @property {number} ERROR - error state
 */
export enum Status {
  LOADING,
  LOADED,
  ERROR
}